<!doctype html>
<html lang="en">

<head>
    {% include "meta.liquid" %}
    <title>{{ page.title }} | LFE Blog</title>
    <meta name="description" content="Browse the LFE blog archive by year.">

    {% include "favicon.liquid" %}
    {% include "css.liquid" %}
</head>

<body class="dark:bg-slate-900">
    <header class="top-nav">
    {% include "nav.liquid" %}
    </header>

    <main id="content" role="main">
      <section class="blog-home-section">

        {% if paginator.indexes %}
          <h1 class="blog-section-heading">{{ page.data.long_title }}</h1>
          <div class="blog-tax-grid">
            {% for sub in paginator.indexes %}
              <a href="/{{ sub.index_permalink }}" class="blog-tax-card">
                <span class="blog-tax-name">{{ sub.index_title }}</span>
                <span class="blog-tax-count">{{ sub.total_pages }}</span>
              </a>
            {% endfor %}
          </div>
        {% else %}
          <h1 class="blog-section-heading">{{ paginator.index_title }}</h1>
          <div class="blog-tax-posts">
            {% for post in paginator.pages %}

              {% assign author_name = post.data.author %}
              {% for author_entry in site.data.authors %}
                {% if author_entry[0] == post.data.author %}
                  {% assign author_name = author_entry[1].name %}
                {% endif %}
              {% endfor %}

              <a href="/{{ post.permalink }}" class="blog-tax-post-item">
                <span class="blog-card-category">{% for cat in post.categories %}{% if forloop.first %}{{ cat }}{% endif %}{% endfor %}</span>
                <h3 class="blog-story-title">{{ post.title }}</h3>
                {% if post.description != "" %}
                <p class="blog-story-desc">{{ post.description }}</p>
                {% endif %}
                <p class="blog-card-meta">{{ author_name }} · {{ post.published_date | date: "%B %-d, %Y" }}</p>
              </a>

            {% endfor %}
          </div>

          {% if paginator.total_indexes > 1 %}
          <nav class="blog-pagination">
            {% if paginator.previous_index %}
              <a href="/{{ paginator.previous_index_permalink }}" class="blog-pagination-link">&larr; Newer</a>
            {% endif %}
            <span class="blog-pagination-info">Page {{ paginator.index }} of {{ paginator.total_indexes }}</span>
            {% if paginator.next_index %}
              <a href="/{{ paginator.next_index_permalink }}" class="blog-pagination-link">Older &rarr;</a>
            {% endif %}
          </nav>
          {% endif %}
        {% endif %}

      </section>
    </main>

    {% include "foot.liquid" %}

    {% include "end-scripts.liquid" %}
</body>

</html>
